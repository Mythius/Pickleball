/* eslint-disable */
import ScheduledGame from './ScheduledGame';

export default {
  title: "ScheduledGame",
};

export const Default = () => <ScheduledGame />;

Default.story = {
  name: 'default',
};
